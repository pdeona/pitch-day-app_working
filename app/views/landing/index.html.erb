

<%= notice %>
<% if @current_user.github_id.nil? %>
  <!-- <%= button_tag link_github_button, remote: true %> -->
<% else %>
  <%= @current_user.trello_id %>
  <%= @current_user.github_id %>
<% end %>
