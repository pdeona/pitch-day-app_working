<p id="notice"><%= notice %></p>

<h1>Projects</h1>

    <% @projects.each do |project| %>
      <p>
        <%= link_to project.name, projects_path, remote: true %>
      </p>
    <% end %>

<br>

<%= link_to 'New Project', new_user_project_path(@user, @project) %>
